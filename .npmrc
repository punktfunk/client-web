@punktfunk:registry=https://git.unom.io/api/packages/unom/npm/
@unom:registry=https://git.unom.io/api/packages/unom/npm/
; @unom/style 0.4.4 still declares motion ^12 as a peer while @unom/ui 0.10 wants ^13. The
; console installs with bun, which tolerates that; npm refuses. Drop this once @unom/style moves.
legacy-peer-deps=true
