{
  "name": "punktfunk-client-web",
  "private": true,
  "type": "module",
  "description": "The punktfunk browser client: @punktfunk/stream (the engine, as a library) and the web app on it.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @punktfunk/stream && npm run build -w punktfunk-web",
    "check": "npm run check --workspaces",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev -w punktfunk-web"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
